{
  "name": "root",
  "private": true,
  "scripts": {
    "graph": "npx nx graph",
    "build:all": "npx lerna run build",
    "run:remixapp": "npx lerna run dev --scope=remixapp",
    "test:all": "npx lerna run test",
    "publish:all:private": "npx lerna publish",
    "publish:all:public": "npx lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^5.4.3",
    "nx": "^14.5.10"
  },
  "workspaces": [
    "packages/*"
  ]
}
