{
  "name": "root",
  "private": true,
  "scripts": {
    "graph": "npx nx graph",
    "build:all": "npx lerna run build",
    "test:all": "npx lerna run test --no-sort"
  },
  "devDependencies": {
    "lerna": "^5.4.3",
    "nx": "^14.5.10"
  },
  "workspaces": [
    "packages/*"
  ]
}
